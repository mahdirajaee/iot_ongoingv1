# Server configuration
HOST=0.0.0.0
PORT=8003

# Resource Catalog
RESOURCE_CATALOG_URL=http://localhost:8001/catalog

# JWT Secret Key (should be a strong, random key in production)
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRATION=3600  # Token expiration time in seconds (1 hour)

# Basic Auth Credentials (temporary until Firebase implementation)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=secure_password_here